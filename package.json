{
  "name": "tsai-mind",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.33.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev:server": "pnpm --filter @tsai-mind/server dev",
    "dev:web": "pnpm --filter @tsai-mind/web dev"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
